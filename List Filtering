# https://www.codewars.com/kata/list-filtering/train/python

def filter_list(l):
    final = []
    for elem in l:
        if type(elem) is int:
            final.append(elem)
    return final       
